@model MovieVM

@{
    ViewData["Title"] = "Create Movie";
}

<div class="container mt-4">
    <h2 class="mb-4">Create New Movie</h2>

    <form asp-action="Create" method="post" enctype="multipart/form-data" class="bg-light p-4 rounded shadow-sm">
        <div class="row mb-3">
            <div class="col-md-6">
                <label class="form-label">Title</label>
                <input name="Name" class="form-control" placeholder="Enter movie title" />
            </div>

            <div class="col-md-6">
                <label class="form-label">Release Date</label>
                <input name="DateTime" type="date" class="form-control" />
            </div>

            <div class="col-md-6">
                <label class="form-label">Price</label>
                <input name="Price" type="number" class="form-control" />
            </div>

            <div class="mb-3">
                <label for="status" class="form-label">Status</label>
                <select name="Status" class="form-select">
                    <option value="0" selected>ComingSoon</option>
                    <option value="1">NowShowing</option>
                    <option value="2">Finished</option>
                </select>
            </div>
        </div>

        <div class="mb-3">
            <label class="form-label">Description</label>
            <textarea name="Description" class="form-control" rows="3" placeholder="Write a short description..."></textarea>
        </div>

        <div class="row mb-3">
            <div class="col-md-4">
                <label class="form-label">Category</label>
                <select name="CategoryId" class="form-select">
                    <option value="">-- Select Category --</option>
                    @foreach (var item in Model.categories)
                    {
                        <option value="@item.Id">@item.Name</option>
                    }
                </select>
            </div>

            <div class="col-md-4">
                <label class="form-label">Cinema</label>
                <select name="CinemaId" class="form-select">
                    <option value="">-- Select Cinema --</option>
                    @foreach (var item in Model.cinemas)
                    {
                        <option value="@item.Id">@item.Name</option>
                    }
                </select>
            </div>

            <div class="col-md-4">
                <label class="form-label">Actors</label>
                <div class="border rounded p-2" style="max-height: 160px; overflow-y: auto;">
                    @foreach (var actor in Model.Actors)
                    {
                        <div class="form-check">
                            <input type="checkbox" name="actorIds" value="@actor.Id" class="form-check-input" id="act_@actor.Id" />
                            <label class="form-check-label" for="act_@actor.Id">@actor.Name</label>
                        </div>
                    }
                </div>
            </div>

        </div>

        <div class="row mb-3">
            <div class="col-md-6">
                <label class="form-label">Main Image</label>
                <input type="file" name="MainImg" class="form-control" accept="image/*" />
            </div>

            <div class="col-md-6">
                <label class="form-label">Sub Images</label>
                <input type="file" name="SubImages" class="form-control" multiple accept="image/*" />
            </div>
        </div>

        <div class="mt-4 d-flex justify-content-between">
            <a href="/Admin/Movie" class="btn btn-secondary">Back to List</a>
            <button type="submit" class="btn btn-success px-4">Create Movie</button>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
<!-- Bootstrap 5 Icons -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
